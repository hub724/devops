prog 1:
installation and setup

$ sudo apt update
$ sudo apt install openjdk-17-jdk
$ sudo apt install eclipse
$ sudo apt install maven -y
$ mvn version


cradle installation

google: greencloud how to install gradle in ubuntu..(copy paste)

$ wget..........
$ sudo apt install unzip
$ sudo unzip -d.........
$ls /opt/gradle
$ sudo nano...
	export.......
	cntrl x, y to save
$ sudo chmode........
$ source /etc/.....
$gradle -v

prog 3:

mkdir ... cd...
$ grade init
application....java...no...groovy...yes...junit4...projrct name:gradletest1,,,source package:gradletest1-->(BUILD SUCCESS)

